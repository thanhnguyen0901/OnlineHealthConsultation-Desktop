<Window
    x:Class="OnlineHealthConsultation.Desktop.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Doctor Desktop Login"
    Width="460"
    Height="450"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
    <Grid Margin="28" Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Style="{StaticResource TitleText}" Text="Doctor Workstation" />
            <TextBlock
                Margin="0,8,0,24"
                Style="{StaticResource MutedText}"
                Text="WPF/C# client calling the shared backend REST API" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Panel}">
            <StackPanel>
                <TextBlock Style="{StaticResource LabelText}" Text="Doctor Email" />
                <TextBox x:Name="Email" Margin="0,6,0,16" />

                <TextBlock Style="{StaticResource LabelText}" Text="Password" />
                <TextBox x:Name="Password" Margin="0,6,0,22" />

                <Button
                    x:Name="Login"
                    Height="42"
                    Content="Sign In"
                    Style="{StaticResource PrimaryButton}" />

                <TextBlock
                    Margin="0,18,0,0"
                    Style="{StaticResource SuccessText}"
                    Text="{Binding StatusMessage}" />
                <TextBlock
                    Margin="0,18,0,0"
                    Style="{StaticResource ErrorText}"
                    Text="{Binding ErrorMessage}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
