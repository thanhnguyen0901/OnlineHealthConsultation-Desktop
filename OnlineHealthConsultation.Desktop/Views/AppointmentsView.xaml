<UserControl
    x:Class="OnlineHealthConsultation.Desktop.Views.AppointmentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Grid Margin="0,0,0,18" DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource PageTitleText}" Text="Appointments" />
                <TextBlock Style="{StaticResource MutedText}" Text="Manage doctor appointment workflow from the desktop client." />
            </StackPanel>
            <ComboBox
                x:Name="StatusFilter"
                Grid.Column="1"
                Margin="12,0"
                VerticalAlignment="Center"
                ItemsSource="{Binding StatusFilters}"
                SelectedItem="{Binding StatusFilter}" />
            <Button
                x:Name="Load"
                Grid.Column="2"
                Content="Refresh"
                Style="{StaticResource SecondaryButton}" />
            <Button
                x:Name="ConfirmSelected"
                Grid.Column="3"
                Content="Confirm"
                Style="{StaticResource SecondaryButton}" />
            <Button
                x:Name="CompleteSelected"
                Grid.Column="4"
                Content="Complete"
                Style="{StaticResource PrimaryButtonSmall}" />
        </Grid>

        <TextBlock
            Margin="0,12,0,0"
            DockPanel.Dock="Bottom"
            Style="{StaticResource ErrorText}"
            Text="{Binding ErrorMessage}"
            TextWrapping="Wrap" />

        <DataGrid
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            GridLinesVisibility="None"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding Appointments}"
            SelectedItem="{Binding SelectedAppointment}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding ScheduledAtText}"
                    Header="Time" />
                <DataGridTextColumn
                    Width="180"
                    Binding="{Binding PatientName}"
                    Header="Patient" />
                <DataGridTextColumn
                    Width="160"
                    Binding="{Binding Status}"
                    Header="Status" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Reason}"
                    Header="Reason" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Notes}"
                    Header="Notes" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
